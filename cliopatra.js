
"use strict" 

/*/////////////////////////////////////////////////////////////////////////////
// Includes 
/////////////////////////////////////////////////////////////////////////////*/

var clio = require('./src').clio;

console.log( clio );

clio.option('-s --short !', 'flag', 'This is a shortflag' );
clio.parse();