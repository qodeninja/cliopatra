
"use strict" 

/*/////////////////////////////////////////////////////////////////////////////
// Includes 
/////////////////////////////////////////////////////////////////////////////*/

var clio = require('./src').clio;

console.log( clio );

clio.enable('autoshort').option('--short ! <arg1> <arg2>', 'flag', 'This is a shortflag' );
clio.parse();